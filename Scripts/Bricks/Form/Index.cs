// Bricks/Form Index
// Re-exports all form management bricks for discovery

namespace TokuTactics.Bricks.Form
{
    // CheckFormAvailability - Returns availability status for a form + ranger (equipped, cooldown, occupied)
    // ValidateFormEquip - Checks if a form can be equipped (budget, lock, registration)
    // TickCooldownWithRegen - Ticks one cooldown and applies health regen if still cooling
}
