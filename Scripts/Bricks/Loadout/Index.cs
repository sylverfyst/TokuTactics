// Bricks/Loadout Index
// Re-exports all loadout bricks for discovery

namespace TokuTactics.Bricks.Loadout
{
    // ValidateMorphRequest - Checks if a ranger can morph (unmorphed, alive, loadout state)
    // ValidateLoadoutSubmission - Validates form selections (locked, empty, budget, registered)
}
